import React from 'react'

const DeleteWordModal = ({ word }) => {
    return (
        <div>
            Are you sure to delete word <b>{word}</b> from dictionary?
        </div>
    )
}

export default DeleteWordModal
