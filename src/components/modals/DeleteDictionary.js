import React from 'react'

const DeleteDictionaryModal = ({ name }) => {
    return (
        <div>
            Are you sure to delete dictionary <b>{name}</b>?
        </div>
    )
}

export default DeleteDictionaryModal
